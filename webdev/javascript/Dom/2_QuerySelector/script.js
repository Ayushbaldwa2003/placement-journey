// query selector is used to select the first element that matches the specified selector. It returns the first element that matches the specified selector, or null if no matches are found.
// query selector all is used to select all elements that match the specified selector. It returns a static NodeList of all elements that match the specified selector, or an empty NodeList if no matches are found.
// const body=document.querySelector('body');
// body.innerHTML="<h1>Hacked!!</h1>";


// const code=document.querySelector('code');
// code.innerText="console.log('Hacked!!')";



const username=document.querySelector('#username');
username.innerText="Ayush Baldwa!!";

